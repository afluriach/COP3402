int in;
procedure int fibo(a);
begin
	if a = 0 then return 0;
	if a = 1 then return 1;
	if a > 1 then return fibo(a-1) + fibo(a-2);
end;

begin
/* input a negative value to end the program */
	read(in);
	write(in);
	while in >= 0 do 
	begin
		write(fibo(in));
		read(in);
	end
end.
